---
name: implement
disable-model-invocation: true
description: "Implement a feature or increment locally, human-driven (implement-feature)"
argument-hint: "[spec path | issue | description]"
---
# implement — supervised alias

This skill is an explicit user-invoked alias for the supervised Owner workflow.
Read and follow `.agents/skills/implement-feature/SKILL.md` in full, using the
current argument as its task input. Do not select an orchestrated or unattended
mode unless the user explicitly invoked that mode instead.
